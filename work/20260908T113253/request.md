# Request

Reconcile entry points, CLI help, runbook guidance, and release documentation with the
  implemented pipeline status, explicit evaluation stages, pinned model, and checkpoint
  compatibility rules.
